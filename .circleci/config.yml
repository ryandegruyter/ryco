version: 2.1
jobs:
  build:
    docker:
      - image: 'circleci/node:latest'
    steps:
      - checkout
      - which google-chrome
      - run: npm ci
      - run: npm test
      - run: npm chromatic -a CHROMATIC_PROJECT_TOKEN --exit-zero-on-changes
